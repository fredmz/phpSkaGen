import { Injectable } from '@angular/core';
import { Http, Response } from '@angular/http';
import { Observable } from 'rxjs/Rx';{{imports}}
import { {{domainClass}} } from './{{domainFile}}'
import { ResponseWrapper, createRequestOption } from '../../../shared';

@Injectable()
export class {{domainClass}}Service {
    private resourceUrl = '{{url}}';

    constructor(private http: Http) { }

    create({{domainObject}}: {{domainClass}}): Observable<{{domainClass}}> {
        const copy = this.convert({{domainObject}});
        return this.http.post(this.resourceUrl, copy).map((res: Response) => {
            return res.json();
        });
    }

    update({{domainObject}}: {{domainClass}}): Observable<{{domainClass}}> {
        const copy = this.convert({{domainObject}});
        return this.http.put(this.resourceUrl, copy).map((res: Response) => {
            return res.json();
        });
    }

    find(id: number): Observable<{{domainClass}}> {
        return this.http.get(`${this.resourceUrl}/${id}`).map((res: Response) => {
            return res.json();
        });
    }

    query(req?: any): Observable<ResponseWrapper> {
        const options = createRequestOption(req);
        return this.http.get(this.resourceUrl, options)
            .map((res: Response) => this.convertResponse(res));
    }

    delete(id: number): Observable<Response> {
        return this.http.delete(`${this.resourceUrl}/${id}`);
    }

    private convertResponse(res: Response): ResponseWrapper {
        const jsonResponse = res.json();
        return new ResponseWrapper(res.headers, jsonResponse, res.status);
    }

    private convert({{domainObject}}: {{domainClass}}): {{domainClass}} {
        const copy: {{domainClass}} = Object.assign({}, {{domainObject}});
        return copy;
    }
}
